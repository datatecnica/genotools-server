apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.appservices.genotrackerApp.podName }} 
  name: {{ .Values.appservices.genotrackerApp.podName }} 
  namespace: {{ .Values.namespace }} 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.appservices.genotrackerApp.podName }} 
  template:
    metadata:
      labels:
        app: {{ .Values.appservices.genotrackerApp.podName }} 
      annotations:
        gke-gcsfuse/volumes: "true" 
    spec:
      serviceAccountName: {{ .Values.ksaBucket }} 
      containers:
      - image: {{ .Values.appservices.genotrackerApp.image }}
        name: {{ .Values.appservices.genotrackerApp.containerName }} #containerName gp2-browser-container
        resources:
          requests:
            cpu: "125m"
            memory: "1Gi"
          limits:
            cpu: "250m"
            memory: "2Gi"        
        ports:
        - containerPort: {{ .Values.appservices.genotrackerApp.containerPort | int  }} #8080
        volumeMounts:
        - name: {{ .Values.appservices.genotrackerApp.volume }} #gcs-volume
          mountPath: {{ .Values.appservices.genotrackerApp.mountPath }} 
      volumes:
      - name: {{ .Values.appservices.genotrackerApp.volume }} #gcs-volume
        persistentVolumeClaim:
          claimName: {{ .Values.persistentVOLUMECLAIM }}
      # Ensure pods are scheduled on nodes with the specified GCP VM type
      nodeSelector:
        cloud.google.com/gke-nodepool: {{ .Values.nodePools.appsNodePool }} 
        iam.gke.io/gke-metadata-server-enabled: "true"        
